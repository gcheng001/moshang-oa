# Windows 使用托盘式审批值守程序

Windows 版在用户登录系统后自动启动审批值守，关闭主窗口后仍在系统托盘运行，并提供状态、自动审批开关和彻底退出入口。开机和登录只启动监测，自动写入默认关闭，必须由当前审批账号显式打开开关；开关状态不在账号之间继承。开关开启期间全天运行，登录失效、租约丢失或网络异常时立即停止自动写入。OA 登录凭据按账号存入操作系统凭据库而非浏览器存储，具体登录方式由 ADR-0010 约束。这样可以长期值守，同时避免登录、开机或切换账号后未经授权自动改变案件状态。
