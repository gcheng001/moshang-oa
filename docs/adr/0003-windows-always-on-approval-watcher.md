# Windows 使用托盘式审批值守程序

Windows 版是主要审批主机，在用户登录系统后自动启动，关闭主窗口后仍在系统托盘值守；Mac 版提供同样的后台入口，主要用于查看和在 Windows 不可用时人工接管。自动写入默认关闭，必须由审批账号显式开启；登录失效或网络异常只停止本轮判断，不得把技术异常当成业务理由驳回。OA 登录凭据和可选飞书密钥存入操作系统凭据库，可通过一次性迁移码加密转移到另一台电脑。
