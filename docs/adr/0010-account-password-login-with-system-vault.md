# 使用账号密码登录并由系统凭据库保持登录

办公助手面向所有 OA 用户提供账号密码登录，不再要求用户获取或输入 API Key；登录成功后使用 OA 返回的访问令牌，任何账号、密码、API Key 或长期令牌均不得保存到浏览器存储。用户显式勾选“保持登录”时，密码按 OA 账号保存到 macOS 钥匙串或 Windows 凭据管理器，用于令牌失效后的自动重登；未勾选时只维持当前会话。自动审批需要长期有效的账号身份，因此只有开启“保持登录”的审批账号才能开启自动审批。
