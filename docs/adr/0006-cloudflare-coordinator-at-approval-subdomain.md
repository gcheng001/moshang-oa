# 协调服务部署在 approval.gaodazhuang.top

审批协调服务使用 Cloudflare Worker 和 D1，并绑定 `approval.gaodazhuang.top`；当前域名已由 Cloudflare 托管 DNS，且没有需要保留的同名解析或既有服务器。Cloudflare 只保存租约与审计元数据，Mac 和 Windows 的连通性在三天影子运行期间共同验证，案件读取与审批写入继续直连摩尚 OA。
